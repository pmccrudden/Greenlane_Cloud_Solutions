FROM node:20-alpine

# Create app directory
WORKDIR /app

# No need to copy package.json - just focusing on the app
COPY app-minimal.mjs .

# Set environment variables
ENV NODE_ENV=production
ENV PORT=8080

# Expose the port
EXPOSE 8080

# Start the application with the ESM version
CMD ["node", "app-minimal.mjs"]